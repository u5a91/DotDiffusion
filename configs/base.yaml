data_dir: data/cifar10/train
image_size: 16

batch_size: 8
num_workers: 0

epochs: 20
learning_rate: 0.0001

base_channels: 32
time_emb_dim: 64

timesteps: 100
beta_start: 0.0001
beta_end: 0.02

seed: 0
device: cpu

save_every: 5
sample_count: 4

checkpoint_dir: checkpoints
output_dir: outputs
